[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "reqPlus"
version = "0.1.0"
description = "requestsSessionFlow"
dependencies = [
    "lxml",
    "browserforge[all]",
    "requests"
]
